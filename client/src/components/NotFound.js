{/* component renders a message letting the user know that the requested page can't be found */}

const NotFound = () => (
    
    <div className="wrap">
        <h2>Not Found</h2>
        <p>Sorry! We couldn't find the page you're looking for.</p>
    </div>
);

export default NotFound;